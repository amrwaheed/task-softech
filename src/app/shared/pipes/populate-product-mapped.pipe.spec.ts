import { PopulateProductMappedPipe } from './populate-product-mapped.pipe';

describe('PopulateProductMappedPipe', () => {
  it('create an instance', () => {
    const pipe = new PopulateProductMappedPipe();
    expect(pipe).toBeTruthy();
  });
});
